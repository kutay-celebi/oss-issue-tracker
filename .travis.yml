matrix:
  include:
    - language: java
      sudo: required
      before_install:
        - "cd api"
        - chmod +x gradlew
        - chmod +x gradle/wrapper/gradle-wrapper.jar
        - gradle wrapper
#notifications:
#  email:
#    - yourname@gmail.com