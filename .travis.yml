matrix:
  include:
    - stage: auth
      language: java
      sudo: required
      before_install:
        - "cd auth"
        - chmod +x gradlew
        - chmod +x gradle/wrapper/gradle-wrapper.jar
        - gradlew build

    - stage: api
      language: java
      sudo: required
      before_install:
        - "cd api"
        - chmod +x gradlew
        - chmod +x gradle/wrapper/gradle-wrapper.jar
        - gradlew build
notifications:
  email:
    - mail@kutaycelebi.com